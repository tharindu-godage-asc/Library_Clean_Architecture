version: '3.9'

services:
  postgres:
    image: postgres:16
    container_name: library-db
    restart: always
    environment:
      POSTGRES_DB: librarydb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"